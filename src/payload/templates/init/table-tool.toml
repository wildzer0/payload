[defaults]
writer = "bin"
output_dir = "build"
cache_dir = ".payload_cache"
byte_order = "little"

[toolchain]
compiler = "gcc"
compiler_flags = []
objcopy = "objcopy"
# Only required if you use the 'obj' writer (compiles .c -> linkable .o).
# objcopy_target = "elf32-littlearm"   # bfd target of your MCU
# objcopy_arch = "arm"                 # architecture for objcopy -B
