name: test

on:
  push:
  pull_request:

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ["3.10", "3.11", "3.12"]

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Installa il pacchetto (con extra dev e serve)
        run: pip install -e ".[dev,serve]"

      - name: pld --version
        run: pld --version

      - name: pld doctor (deve girare anche senza toolchain configurato)
        run: pld doctor
        continue-on-error: true  # doctor può segnalare FAIL su compiler/objcopy assenti, atteso su CI pulita

      - name: Test suite
        run: pytest -v
